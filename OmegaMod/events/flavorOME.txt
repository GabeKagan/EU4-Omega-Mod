#Events for the Omega

namespace = flavor_ome

#Turns a random AI country into the Omega
country_event = {
    id = flavor_ome.1
    title = flavor_ome.1.t
    description = flavor_ome.1.d
    picture = RELIGIOUS_TURMOIL_eventPicture #Replace
    fire_only_once = yes

    trigger = {
        NOT = { exists = OME }
        is_year = 1444
        ai = yes #Because turning a human into the Omega would be unfun.
    }

    mean_time_to_happen = {
        days = 1 #Rewrite this so it happens on a specific day?
    }

    option = { 
        name = flavor_ome.1.a #Localization for later: Let the reign of terror begin!

        every_province = {
            limit = {
                owned_by = ROOT
                }
            add_core = OME #Omega should get cores on its "host" nation 
        }

        change_technology_group = omegan
        change_tag = OME
        change_unit_type = chinese #Placeholder
        swap_free_idea_group = yes      #keep progress (Get the Omega ideas?)

        #If the Omega is tribal, turn it into a monarchy and remove any native councils
        if {
            owner = {
                OR = { government = tribal_despotism government = tribal_kingdom government = tribal_federation government = steppe_horde government = siberian_native_council government = tribal_democracy government = native_council }
            }
            change_government = despotic_monarchy
            native_mechanic = no #Just a test!
        }
    }
}

#Anti-protectorate event
country_event = {
    id = flavor_ome.2
    title = flavor_ome.2.t
    description = flavor_ome.2.d
    picture = RELIGIOUS_TURMOIL_eventPicture

    trigger {
        tag = OME
    }

    mean_time_to_happen = {
        months = 30 #Adjust as needed?
    }

    option = { 
        name = flavor_ome.2.a #Localization: We must refrain from indirect rule
        option = {
            random_subject_country = {
                is_protectorate = {
                    PREV = { vassalize = PREV } #Should ensure diplomatic annexation at some point, at the very least
                }
            }
        }
    }
}

#Free explorers for the Omega once it has ships (based off "Merchant Expedition in RandomEvents.txt"), even if it doesn't take exploration ideas

country_event = {
    id = flavor_ome.3
    title = flavor_ome.3.t
    desc = flavor_ome.3.d
    picture = EXPLORERS_eventPicture
    
    trigger = {
        num_of_ports = 1
        tag = OME
    }
    
    mean_time_to_happen = {
        months = 120
    }
    
    option = {
        name = flavor_ome.3.a
        random_owned_province = {
            limit = { has_port = yes }
            heavy_ship = ROOT
        }
        create_explorer = 50
    }
}

